/**
 * Restaurant Service Configuration
 * Centralizes all settings used by restaurantService.js.
 */
export const restaurantServiceConfig = {
    // The API endpoint of our backend (update this for production)
    BACKEND_API_BASE: "http://localhost:8000",
};
