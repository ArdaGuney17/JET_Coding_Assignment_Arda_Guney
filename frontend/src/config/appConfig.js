/**
 * Global Frontend Configuration
 * Centralizing all external URLs and constants for easy maintenance.
 */
export const AppConfig = {
    // The base URL for the Google Maps embed
    MAPS_BASE_URL: "https://maps.google.com/maps",
    
    // Default zoom level for our mini-maps
    MAPS_DEFAULT_ZOOM: 15,

    // The API endpoint of our backend (can be updated for production)
    BACKEND_API_BASE: "http://localhost:8000"
};
